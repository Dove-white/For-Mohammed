# 📝 Quiz: Semantic Elements

Here are 5 questions based on the **Semantic Elements** article.

1. What is the purpose of using semantic HTML elements like `<header>` and `<footer>`?
2. Name two benefits of semantic HTML for SEO.
3. How does `<main>` differ from `<section>`?
4. Give an example of when to use `<aside>`.
5. Why is it important to include `alt` text for images when following semantic HTML practices?


## Instructions

1. Answer all questions above.
2. Once you finish, **record your answers (video or audio)**.
3. Share your results with your instructor for review.