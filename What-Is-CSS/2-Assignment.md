# 📝 Assignment: CSS — Cascade & Specificity

## Instructions

1. Create a new folder and open it in **VS Code**.
2. Inside the folder, create two files: `index.html` and `styles.css`.
3. Set up the full HTML structure with three `<p>` elements and link the external CSS file.
4. In your CSS file, create **three conflicting style rules**, one for each `<p>` element:

   * One using an element selector
   * One using a class selector
   * One using an ID selector
6. In your HTML file, add a comment explaining **why that rule wins** (based on the cascade & specificity).
7. Make sure the page title is your full name
8. Preview your webpage in the browser.
9. **Take a screenshot of VS Code + the webpage**.
10. Submit your screenshot to your instructor for review.

Happy coding 🚀!
