# 📝 Quiz: Global Attributes in HTML

Here are 5 questions based on the **Global Attributes** article.

1. What is the main purpose of the `id` attribute in HTML?
2. How is the `class` attribute different from `id`, and why would you use multiple classes?
3. What does the `title` attribute do, and when might it be helpful?
4. How can you access a `data-*` attribute in JavaScript?
5. Why is using the `style` attribute directly on elements generally discouraged in favor of CSS stylesheets?




## Instructions

1. Answer all questions above.
2. Once you finish, **record your answers (video or audio)**.
3. Share your results with your instructor for review.