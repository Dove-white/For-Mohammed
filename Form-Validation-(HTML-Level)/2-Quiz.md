# 📝 Quiz: Form Validation (HTML Level)

Here are 5 questions based on the **Form Validation** article.

1. What is the purpose of the HTML `required` attribute?
2. How does the `pattern` attribute work and when would you use it?
3. Why should you use a `<label>` element instead of relying only on a placeholder?
4. Provide an example of an input field that requires a password with at least one uppercase, one lowercase, and one number.
5. How does the browser behave when a required field is left empty and the form is submitted?



## Instructions

1. Answer all questions above.
2. Once you finish, **record your answers (video or audio)**.
3. Share your results with your instructor for review.