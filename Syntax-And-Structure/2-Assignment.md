## 🧪 Practical Coding Assignment

Create a small HTML file and apply CSS based on what you learned. Follow the tasks below:

### Apply Different Selectors

Style the following HTML using **element**, **class**, **ID**, and **attribute** selectors.

```html
<div id="container">
  <h1 class="title">Welcome to CSS Practice</h1>
  <p class="info">This is a paragraph that needs styling.</p>
  <input type="text" placeholder="Type here...">
  <p class="note">This is a note.</p>
</div>
```

### ❗ Your CSS should:

* Change the color of **all ****************`<p>`**************** tags** using an **element selector**
* Change the background of `.note` using a **class selector**
* Add a border to `#container` using an **ID selector**
* Style the text input using an **attribute selector** like:

  ```css
  input[type="text"] { ... }
  ```

## Instructions

1. Make sure the page title is your full name
2. Preview your webpage in the browser.
3. **Take a screenshot of VS Code + the webpage**.
4. Submit your screenshot to your instructor for review.

Happy coding 🚀!

