# 📝 Quiz: Advanced CSS Selectors

Here are 5 questions based on the **Advanced CSS Selectors** article.

### 1. **Attribute Selector**: Which selector will select all `<input>` elements that have a `required` attribute?

* A. `input[required]`
* B. `[input=required]`
* C. `input.required`
* D. `input#required`

### 2. **Universal Selector**: What is the main use of the `*` selector in CSS?

* A. Select only `<div>` elements
* B. Apply styles to all elements
* C. Select elements with a specific class
* D. None of the above

### 3. **Child Selector**: Given `<ul><li>Item</li></ul>`, which selector targets only the `<li>` inside `<ul>`?

* A. `ul li`
* B. `ul > li`
* C. `li > ul`
* D. `li ul`

### 4. **Sibling Selector**: Which selector selects all paragraphs following an `<h2>` element?

* A. `h2 + p`
* B. `h2 p`
* C. `h2 ~ p`
* D. `h2 > p`

### 5. **Combining Selectors**: How would you select all `<a>` elements inside a `<div>` with a `data-active` attribute?

* A. `div a[data-active]`
* B. `div[data-active] a`
* C. `div a.active`
* D. `a div[data-active]`


## Instructions

1. Answer all questions above.
2. Once you finish, **record your answers (video or audio) explaining your thought process**.
3. Share your results with your instructor for review.